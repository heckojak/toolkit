Simple WebBluetooth implementation for downloading raw dive logs.
Limitation is a web browser supporting the WebBluetooth API, which is currently Google Chrome (excluding iPhone ecosystem) or Microsoft Edge.
For Apple users, tested using Google Chrome on macOS 22+, should work on macOS 15+ as well or wherever is the breaking line in the bluetooth API.
